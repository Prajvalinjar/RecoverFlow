HOST=127.0.0.1
PORT=8000
ENVIRONMENT=development
ALLOWED_ORIGINS=http://localhost:3000

# Database Configuration (PostgreSQL / SQLite fallback for local testing)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/recoverflow
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# Security / Authentication
EVENT_AUTH_SECRET=dev_event_secret_key_123
